---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  labels:
    app.kubernetes.io/component: network-attachment-definition
    app.kubernetes.io/name: local
    app.kubernetes.io/part-of: cluster
  name: cluster
spec:
  config: |
    {
      "name": "vhost0",
      "type": "ptp",
      "ipam": {
        "type": "host-local",
        "subnet": "10.20.0.0/24",
        "routes": [
          {
            "dst": "10.16.0.0/14"
          }
        ]
      }
    }
---
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  labels:
    app.kubernetes.io/component: network-attachment-definition
    app.kubernetes.io/name: local
    app.kubernetes.io/part-of: cluster
  name: local
spec:
  config: |
    {
      "name": "vlan0",
      "type": "macvlan",
      "ipam": {
        "type": "dhcp"
      }
    }
