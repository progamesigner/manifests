---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: syncthing
    app.kubernetes.io/name: syncthing
    app.kubernetes.io/part-of: cluster
  name: syncthing
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: syncthing
      app.kubernetes.io/name: syncthing
      app.kubernetes.io/part-of: cluster
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: syncthing
        app.kubernetes.io/name: syncthing
        app.kubernetes.io/part-of: cluster
    spec:
      automountServiceAccountToken: false
      containers:
      - name: syncthing
        image: syncthing:latest
        ports:
        - name: http
          containerPort: 8384
        - name: syncthing
          containerPort: 22000
        - name: syncthing-udp
          containerPort: 22000
          protocol: UDP
        - name: discovery-udp
          containerPort: 21027
          protocol: UDP
        volumeMounts:
        - name: syncthing
          mountPath: /var/syncthing
        resources:
          limits:
            cpu: 3000m
            memory: 1Gi
          requests:
            cpu: 300m
            memory: 128Mi
      serviceAccountName: default
      volumes:
      - name: syncthing
        emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: syncthing
    app.kubernetes.io/name: syncthing-relay
    app.kubernetes.io/part-of: cluster
  name: syncthing-relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: syncthing
      app.kubernetes.io/name: syncthing-relay
      app.kubernetes.io/part-of: cluster
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        k8s.v1.cni.cncf.io/networks: '[{"name": "cluster"}, { "name": "local", "mac": "7A:22:25:01:04:10" }]'
      labels:
        app.kubernetes.io/component: syncthing
        app.kubernetes.io/name: syncthing-relay
        app.kubernetes.io/part-of: cluster
    spec:
      automountServiceAccountToken: false
      containers:
      - name: syncthing-relay
        image: syncthing-relay:latest
        args:
        - -global-rate=2000000
        - -provided-by='0x148 <https://0x148.com>'
        ports:
        - name: relaysrv
          containerPort: 22067
        - name: relaysrv-status
          containerPort: 22070
        volumeMounts:
        - name: syncthing-relay
          mountPath: /var/strelaysrv
        resources:
          limits:
            cpu: 300m
            memory: 512Mi
          requests:
            cpu: 50m
            memory: 128Mi
      serviceAccountName: default
      volumes:
      - name: syncthing-relay
        emptyDir: {}
